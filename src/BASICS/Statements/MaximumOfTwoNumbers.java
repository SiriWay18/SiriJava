package BASICS.Statements;

public class MaximumOfTwoNumbers {
    public void max(int r , int  t ){
        if(r>t){
            System.out.println("Maximum number between " + r + " and " + t + " is : " + r );
        }else {
            System.out.println("Maximum number between " + r + " and " + t + " is : " + t );
        }
    }
}
